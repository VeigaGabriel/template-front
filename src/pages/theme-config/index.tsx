import React from 'react';

export default function ThemeConfig() {
  return (
    <section>
      <div>
        <h1>Theme Config</h1>
        <p>Theme Config</p>
        <button>Save Changes</button>
      </div>
    </section>
  );
}
